fastapi==0.115.0
uvicorn==0.30.5
httpx==0.27.2
# ponytail: yt-dlp is deliberately unpinned. YouTube breaks extraction on a
# regular basis, and the normal fix is rebuilding the image against a fresh
# version. Pinning it here would guarantee breakage, not reproducibility.
yt-dlp
# Same reasoning as yt-dlp: ytmusicapi speaks InnerTube from the inside, and
# Google changes that surface without notice. Used only by ytm_sync.py — the
# playback worker never imports it.
ytmusicapi
# The JS challenge solver scripts for yt-dlp's n-challenge (EJS). Has to move
# in lockstep with yt-dlp, hence unpinned for the same reason.
yt-dlp-ejs
